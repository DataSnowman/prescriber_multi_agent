# ── Agent Framework (pinned pre-release) ──────────────────────
agent-framework-azure-ai==1.0.0rc3
agent-framework-core==1.0.0rc3

# ── Agent-as-Server (optional — only needed for --server mode) ─
azure-ai-agentserver-core==1.0.0b17
azure-ai-agentserver-agentframework==1.0.0b17

# ── Azure AI Projects (pinned by agent-framework-core rc3) ────
azure-ai-projects==2.0.0b4

# ── Starlette (agentserver-core 1.0.0b17 requires >=1.0.0rc1) ──
starlette>=1.0.0

# ── Fabric Data Agent client deps ─────────────────────────────
azure-identity>=1.15.0
openai>=1.0.0
python-dotenv>=0.21.1

# ── Production server (ACA) ──────────────────────────────────
gunicorn>=21.2.0

# ── Dev / Debug ───────────────────────────────────────────────
debugpy
agent-dev-cli --pre
